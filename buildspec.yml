 
version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
    commands:
      #- apt install python3
      #- apt install python3-pip
      - echo "salve"
  build:
    commands:
      - echo tentando mover as fotos
     
      

artifacts:
  files:
    #- '*.png'
    #- '**/*'
    - 'index.html'
    - 'scripts/*'
  discard-paths: no
  name: blue-iaas-frontend-artifact
